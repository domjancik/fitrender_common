require 'spec_helper'

describe FitrenderCommon do
  it 'has a version number' do
    expect(FitrenderCommon::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
